# Stove Undo Extension 개선 계획

## 현재 상태

### 구현 완료
- ✅ 기본 Undo/Redo (Ctrl+Z, Ctrl+Y, Ctrl+Shift+Z)
- ✅ 150개 히스토리 버퍼
- ✅ 커서 위치 복원
- ✅ 중복 상태 방지
- ✅ 8가지 에디터 타입 자동 감지
- ✅ 에디트 모드 초기 상태 버그 수정

### 주요 문제점
- ❌ 여러 에디터 간 히스토리 혼재
- ❌ 페이지 새로고침 시 히스토리 소멸
- ❌ 사용자 피드백 부족 (undo/redo 가능 여부 모름)
- ❌ 메모리 누수 가능성 (제거된 에디터 cleanup 없음)
- ❌ 설정 옵션 최소화

---

## Phase 1: 핵심 버그 수정 및 안정성 확보

### 1.1 다중 에디터 독립 히스토리 관리
**문제**: 한 페이지에 여러 에디터가 있을 때 히스토리가 섞임

**구현 내용**:
- 각 에디터에 고유 ID 부여 (data-undo-id)
- WeakMap으로 에디터별 EditorHistoryManager 인스턴스 관리
- 포커스된 에디터의 히스토리만 활성화

**파일 수정**:
- `content.js`: 전역 변수 구조 변경, 에디터 매핑 시스템 추가

**예상 시간**: 3-4시간

---

### 1.2 메모리 누수 방지
**문제**: 에디터가 DOM에서 제거되어도 이벤트 리스너와 히스토리가 남아있음

**구현 내용**:
- MutationObserver로 에디터 제거 감지
- 제거 시 이벤트 리스너 해제 및 히스토리 정리
- WeakMap 사용으로 자동 GC 지원

**파일 수정**:
- `content.js`: cleanup 메서드 추가, 에디터 제거 감지 로직

**예상 시간**: 2-3시간

---

### 1.3 에러 핸들링 및 사용자 피드백
**문제**: 에러 발생 시 사용자에게 알림 없음

**구현 내용**:
- 토스트 알림 컴포넌트 (간단한 CSS 오버레이)
- 복원 실패 시 사용자에게 메시지 표시
- try-catch 블록 강화

**파일 수정**:
- `content.js`: 토스트 함수 추가, 에러 처리 개선
- 새 파일: `toast.css` (간단한 스타일)

**예상 시간**: 2시간

---

## Phase 2: 사용성 개선

### 2.1 Undo/Redo 버튼 UI
**문제**: 사용자가 실행취소 가능 여부를 모름

**구현 내용**:
- 에디터 우측 상단에 플로팅 버튼 추가
- Undo/Redo 가능 여부에 따라 버튼 활성화/비활성화
- 현재 히스토리 위치 표시 (예: 5/10)
- 호버 시 단축키 힌트 표시

**파일 수정**:
- `content.js`: UI 생성 및 업데이트 로직
- 새 파일: `undo-ui.css`

**예상 시간**: 4-5시간

---

### 2.2 고급 설정 옵션
**문제**: 사용자 커스터마이징 불가

**구현 내용**:
- 설정 페이지 생성
- 설정 항목:
  - 히스토리 최대 크기 (50-500, 기본 150)
  - Debounce 시간 (0-1000ms, 기본 100ms)
  - UI 버튼 표시 여부
  - 디버그 모드 on/off
- chrome.storage.sync에 저장
- 실시간 설정 반영 (메시지 패싱)

**파일 추가/수정**:
- 새 파일: `options.html`, `options.js`, `options.css`
- `manifest.json`: options_page 추가
- `content.js`: 설정 읽기 로직
- `popup.html`: 설정 페이지 링크 추가

**예상 시간**: 5-6시간

---

### 2.3 스마트 히스토리 저장
**문제**: 모든 변경이 동일하게 처리됨

**구현 내용**:
- 입력 타입 감지 (타이핑 vs 붙여넣기 vs 대량 삭제)
- 타이핑: 긴 debounce (500ms)
- 붙여넣기/대량 변경: 즉시 저장
- 변경 크기 감지 (100자 이상 변경 = 즉시)

**파일 수정**:
- `content.js`: 동적 debounce 로직, 입력 분류

**예상 시간**: 3시간

---

## Phase 3: 고급 기능 (선택적)

### 3.1 히스토리 타임라인 UI
**내용**:
- 팝업 확장하여 히스토리 목록 표시
- 각 항목: 시간, 변경 내용 미리보기 (첫 50자)
- 클릭으로 특정 시점 복원
- 현재 위치 하이라이트

**파일 수정**:
- `popup.html`, `popup.js`: 타임라인 UI
- `content.js`: 메시지 패싱으로 히스토리 전달

**예상 시간**: 6-8시간

---

### 3.2 히스토리 영속성 (선택적 자동 저장)
**내용**:
- chrome.storage.local에 히스토리 저장
- 설정 옵션으로 on/off (기본 off)
- 페이지 URL + 에디터 ID로 매핑
- 5MB 제한 고려하여 최근 N개만 저장
- 프라이버시 경고 표시

**파일 수정**:
- `content.js`: 자동 저장 및 복원 로직
- `options.html`: 영속성 옵션 추가

**예상 시간**: 5-6시간

---

### 3.3 대용량 콘텐츠 처리
**내용**:
- 콘텐츠 크기 측정 (100KB 이상 경고)
- 대용량은 간단한 diff만 저장
- 이미지 base64 제외 (src URL만)

**파일 수정**:
- `content.js`: 크기 체크, diff 로직

**예상 시간**: 4-5시간

---

## 우선순위 권장

### 최우선 (1주차)
1. **다중 에디터 독립 히스토리** - 핵심 버그
2. **메모리 누수 방지** - 안정성
3. **에러 핸들링** - 사용자 경험

### 2순위 (2주차)
4. **Undo/Redo 버튼 UI** - 가시성
5. **고급 설정 옵션** - 유연성

### 3순위 (3주차)
6. **스마트 히스토리 저장** - 최적화
7. **대용량 콘텐츠 처리** - 성능

### 선택적 (필요시)
8. 히스토리 타임라인 UI
9. 히스토리 영속성
10. 단축키 커스터마이징

---

## 기술 스택

- **JavaScript**: ES6+ (Chrome Extension)
- **Storage**: chrome.storage.sync / local
- **APIs**: Chrome Extension Manifest V3
- **UI**: Vanilla HTML/CSS (간단한 오버레이)
- **Data Structure**: WeakMap (메모리 관리)

---

## 테스트 체크리스트

### 기능 테스트
- [ ] 단일 에디터에서 undo/redo 동작
- [ ] 다중 에디터 간 독립 히스토리
- [ ] 에디터 추가/제거 시 정상 동작
- [ ] 페이지 새로고침 후 정상 초기화
- [ ] 대용량 텍스트 (10만자+) 처리
- [ ] 이미지 포함 rich text 처리

### 성능 테스트
- [ ] 메모리 사용량 모니터링 (1시간 사용)
- [ ] 히스토리 150개 저장 시 속도
- [ ] 페이지 로딩 시간 영향

### 호환성 테스트
- [ ] cafe.naver.com 에디터
- [ ] *.onstove.com 에디터
- [ ] Chrome 최신 버전
- [ ] 다양한 에디터 타입 (Quill, ProseMirror 등)

---

## 완료 기준

- 모든 Phase 1 항목 구현 및 테스트 통과
- 메모리 누수 없음 (1시간 사용 후 확인)
- 다중 에디터 환경에서 정상 동작
- 사용자 피드백 UI 제공
- 설정 옵션으로 기본 기능 커스터마이징 가능
